def divide(num1, num2):
    """
    num1: a number to be divided (numerator)
    num2: a number to be divided by (denominator)
    return: the result of the division of num1 by num2
    """
    if num2 == 0:
        return "Error: Division by zero is not allowed."
    else:
        return num1 / num2
        return result
    
print(divide(num1=10, num2=0))  # Output: 5.0
print(help(divide))  # Output: Displays the docstring for the divide function

























