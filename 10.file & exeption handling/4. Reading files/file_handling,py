file_handler = open("practice.txt",'rt')

# Read operating 
#read ()=> reads the  contents of the file as str 

content = file_handler.read(10) # this reads first 10 characters of the file 
#closinng a file => close()
file_handler.close()


print(content)
print(type(content))